server {
    listen 80;
    listen [::]:80;

    server_name {{ bitwarden_domain_name }};

    root /var/www/{{ bitwarden_domain_name }}/html;

    index updating.html;

    location / {
            try_files $uri $uri/ =404;
    }
}